---
import { Image } from "astro:assets";
import heroImage from "@assets/hero.jpg";
---

<section class="pt-20 pb-16 md:pt-28 md:pb-24 overflow-hidden relative">
  <div class="container-custom relative">
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 items-center">
      <div>
        <h1 class="text-4xl md:text-5xl lg:text-6xl font-bold leading-tight">
          Empowering <span class="gradient-text">Rangatahi</span>
        </h1>
        <p class="mt-6 text-xl text-gray-600 max-w-lg">
          Empowering the next generation through hands on, industry supported,
          Technology Education.
        </p>
        <div class="mt-8 flex flex-col sm:flex-row gap-4">
          <a href="/mahi/sessions" class="btn btn-primary">Join a Session</a>
          <a href="/mahi" class="btn btn-secondary">Get Involved</a>
        </div>
      </div>

      <div class="">
        <div class="relative hover-zoom">
          <div
            class="absolute -inset-0.5 bg-gradient-to-r from-tuhura-green to-tuhura-blue rounded-xl blur opacity-30 transition-opacity hover:opacity-50"
          >
          </div>
          <div class="relative bg-white rounded-xl shadow-xl overflow-hidden">
            <Image
              src={heroImage}
              alt="Tūhura Tech"
              class="w-full h-auto transition-all duration-500"
              width={700}
              height={500}
            />
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
